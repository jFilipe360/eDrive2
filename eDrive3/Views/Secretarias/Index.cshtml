@model IEnumerable<eDrive3.Models.Secretaria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FotoUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NrTelemovel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                    <img src="@item.FotoUrl"
                         alt="Foto de @item.Nome"
                         style="max-height:70px;max-width:70px;object-fit:cover;">
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NrTelemovel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SecretariaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SecretariaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SecretariaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
